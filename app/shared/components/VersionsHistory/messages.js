/*
 * VersionsHistory Messages
 *
 * This contains all the text for the VersionsHistory component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.VersionsHistory'

export default defineMessages({
  versionsHistory: {
    id: `${scope}.versionsHistory`,
    defaultMessage: 'Versions History',
  },
  seeAll: {
    id: `${scope}.seeAll`,
    defaultMessage: 'See All',
  },
})
