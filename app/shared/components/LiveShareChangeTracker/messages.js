/*
 * LiveShareChangeTracker Messages
 *
 * This contains all the text for the LiveShareChangeTracker component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.LiveShareChangeTracker'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LiveShareChangeTracker component!',
  },
})
