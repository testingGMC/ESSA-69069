/*
 * MarkAsModal Messages
 *
 * This contains all the text for the MarkAsModal component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.MarkAsModal'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MarkAsModal component!',
  },
})
