/*
 * GraphContainer Messages
 *
 * This contains all the text for the GraphContainer component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.GraphContainer'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GraphContainer component!',
  },
})
