/*
 * TransformTitle Messages
 *
 * This contains all the text for the TransformTitle component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.TransformTitle'

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Transform',
  },
})
