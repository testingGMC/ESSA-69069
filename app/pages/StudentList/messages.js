/*
 * StudentList Messages
 *
 * This contains all the text for the StudentList container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.StudentList'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentList container!',
  },
})
